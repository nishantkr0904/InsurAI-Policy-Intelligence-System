import ClaimsClient from "./ClaimsClient";

/**
 * Claims validation workflow container.
 * Keeps workflow concerns separate from overview/dashboard concerns.
 */
export default function ClaimsValidationWorkflow() {
  return <ClaimsClient />;
}
